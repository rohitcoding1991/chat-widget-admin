export const COMMON_ROUTES: string[] = ["/"];

export const ROOT: string = "/";

export const PUBLIC_ROUTES: string[] = ["/login", "register", "/auth/error"];

export const DEFAULT_REDIRECT: string = "/dashboard";
